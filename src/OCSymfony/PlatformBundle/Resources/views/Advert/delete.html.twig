{% extends "OCSymfonyPlatformBundle::layout.html.twig" %}

{% block title %}
    Supprimer une annonce - {{ parent() }}
{% endblock %}

{% block ocplatform_body %}
    <h2>Supprimer une annonce</h2>

    <p>Etes vous certain de vouloir supprilmer l'annonce "{{ advert.title }} ?</p>

    <form action="{{ path('oc_symfony_platform_delete', {'id' : advert.id}) }}" method="post">
        <a href="{{ path('oc_symfony_platform_view', {'slug' : advert.slug }) }}" class="btn btn-default">
            <i class="glyphicon glyphicon-chevron-left"></i>
            Retour à l'annonce
        </a>
        <input type="submit" value="Supprimer" class="btn btn-danger" />

        {{ form_rest(form) }}
    </form>
{% endblock %}